apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: vaultwarden
spec:
  descriptor:
    links:
      - description: Open Vaultwarden
        url: 'repl{{ ConfigOption "vaultwarden_domain" }}'
